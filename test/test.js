const assert = require('chai').assert;
const tdd = require('../multiply');

describe('Test for multiply', () => {
    
    it('test 1', () => {
        //Assert is used in here
        assertEqual(multiply(1, 1), 1);
    })

})